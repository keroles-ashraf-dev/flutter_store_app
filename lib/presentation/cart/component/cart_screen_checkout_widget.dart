import 'package:flutter/material.dart';

class CartScreenCheckoutWidget extends StatelessWidget {
  const CartScreenCheckoutWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
